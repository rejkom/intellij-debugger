Refactor the following Java code to use modern Java 21/25 features where it makes sense (for example: records, pattern
matching for switch, enhanced for-loops).
Keep the behavior identical, but improve readability and make the code easier to debug. Do not introduce new frameworks
or libraries that are not already used in this project.